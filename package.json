{
  "name": "gh-workspace-data",
  "version": "0.7.0",
  "description": "Materialize and publish project-associated workspace data through GitHub CLI",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SorinGFS/gh-workspace-data.git"
  },
  "homepage": "https://github.com/SorinGFS/gh-workspace-data#readme",
  "bugs": "https://github.com/SorinGFS/gh-workspace-data/issues",
  "keywords": [
    "github-cli",
    "workspace-data",
    "fixtures",
    "scm"
  ],
  "bin": {
    "gh-workspace-data": "gh-workspace-data"
  },
  "files": [
    "gh-workspace-data",
    "src/**",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "check": "node --check gh-workspace-data && node --check src/index.js && node --check src/version-layers.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
